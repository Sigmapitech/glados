// This is a basic test file
#include <stdbool.h>

int main() {
    int x = 42;
    bool is_valid = true;
    char *message = "Hello World";

    if (x >= 10) {
        return x; /* Return the value
                     if it is big enough */
    }
    return 0;
}
